# Backend uses the ROOT .env file (Karnail/.env).
# Do NOT create backend/.env — edit ../.env instead.
#
# Required vars for API:
#   PORT, MONGODB_URI, FRONTEND_URL, JWT_SECRET, JWT_EXPIRES_IN, COOKIE_SECRET
#   ADMIN_SEED_EMAIL, ADMIN_SEED_PASSWORD (for npm run seed)
#
# See: ../.env.example
